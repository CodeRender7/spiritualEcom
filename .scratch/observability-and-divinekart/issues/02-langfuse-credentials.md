# T2 — Langfuse credentials & project provisioning

- Labels: `wayfinder:task`
- Blocking: blocked-by T1 (done)

## Question

Once the topology is stable, create the org + project + API keys for DivineKart apps, and decide between `LANGFUSE_INIT_*` auto-provision vs manual creation in the UI — plus a secrets-rotation workflow for the two classes of keys (public/secret).

## Background

Langfuse web is live at http://localhost:3000 after T1. The worker is healthy (default model prices seeded), DB connected.

## Resolution

(answer recorded here on close)