# T7 — DivineKart partner platform

- Labels: `wayfinder:grilling`, `wayfinder:task`
- Blocking: blocked-by T5 (per-tenant observability views depend on OpenObserve/Langfuse scoping)

## Question

Scaffold `divinekart-partners` (a new workspace next to `divinekart-backend` / `divinekart-storefront`): partner portal, staff roles, subscribed plans, RBAC admin panel, each tenant scoped to its own observability UI. Decide the DDD vocabulary (Domain entity terms) and the module seam inside the monorepo.

## Resolution

(answer recorded here on close)